{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "netlify-lambda": "^2.0.16",
    "nodemailer": "^6.9.3",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "^5.0.1",
    "serverless-http": "^3.2.0",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "netlify deploy --prod",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "dev": "netlify-lambda serve .netlify/functions",
    "netlify-build": "react-scripts build && netlify deploy --prod"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "sass": "^1.54.5"
  }
}
